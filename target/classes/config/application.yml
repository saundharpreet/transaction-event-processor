project:
  name: "transaction-event-processor"

spring:
  application:
    name: ${project.name}
  profiles:
    active: local

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always